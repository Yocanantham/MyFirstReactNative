import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  view: {
    backgroundColor: 'grey',
    padding: 31,
  },
  text: {color: 'black', textAlign: 'center', fontSize: 31, fontWeight: 700},
});

export default style;
