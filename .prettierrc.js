module.exports = {
    arrowParens: 'avoid',
    bracketSameLine: true,
    bracketSpacing: true,
    singleQuote: true,
    trailingComma: 'all',
    //  tabWidth: 4,
    semi: true,
};

/*
    * Parentheses around a single arrow function parameter
    ++ arrowParens: 'avoid',

    * Put > of closing tags on the last line
    ++ bracketSameLine: true,

    * Add spaces between brackets in object literals
    ++ bracketSpacing: true,

    * Use single quotes instead of double quotes
    ++ singleQuote: true,

    * Add trailing commas wherever possible
    ++ trailingComma: 'all',

    * Number of spaces per indentation++ 
    * Use tabs instead of spaces
    ++ useTabs: false,

    * Max line length before wrapping
    ++ printWidth: 80,

    * Add semicolons at the end of statements
    ++ semi: true,

    * JSX quotes - use double quotes in JSX
    ++ jsxSingleQuote: false,

    * Indent lines in objects, arrays, etc.
    ++ indent: 2,

    * Add new line at the end of files
    ++ endOfLine: 'auto',

    * Parse markdown text as prose
    ++ proseWrap: 'preserve',

*/
